import express from "express";

const router = express.Router()

router.get("/",) // ? query tag // search
router.get("/:id",)
router.put("/put/:id",)
router.post("/add/:id",)
router.delete("/delete/:id")

export default router